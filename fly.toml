# fly.toml app configuration file for pulsekeep
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'pulsekeep'
primary_region = 'lax'

[build]

[env]
  ALLOWED_ORIGIN = '*'
  PORT = '8080'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    grace_period = "15s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/health"

[deployment]
  strategy = "recreate"
  kill_timeout = "10s"
  kill_signal = "SIGTERM"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
